# Tomestone Comments Backend Configuration

# Port to run the server on
PORT=3000

# Path to SQLite database file (relative to backend directory)
DATABASE_PATH=./comments.db

# Comma-separated list of allowed origins for CORS
# Add your extension's origin if needed, or use moz-extension://* for development
ALLOWED_ORIGINS=https://tomestone.gg,moz-extension://*
